---
- name: Tomcat deployment playbook
  hosts: dev
  become: True               # If to escalate privilege
  become_method: sudo       # Set become method
  remote_user: jenkins         # Update username for remote server
  roles:
    - ansible.tomcat8
